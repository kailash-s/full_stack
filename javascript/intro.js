// code is used to give instructions to computers like below for it to run:
alert('hello');

// on console, we can also do maths
console.log(2+2);

// we can also use it to change what is displayed on webpages
document.body.innerHTML = 'hello';

// syntax is important as the code below will cause an error as it's missing the first bracket
alert'hello');